# Apprentice 2.2.2a

- Removes the direct compile-time dependency on `VSSurvivalMod.dll` from the
  custom race appearance system.
- Discovers `CharacterSystem` and the skinnable-player behavior at runtime.
- Restores compatibility with the existing Windows `build.ps1` reference
  staging while preserving race selection and model synchronization.
