--- /mnt/data/apprentice_original/GuiElementSkillTreeCanvas.cs	2026-07-14 00:52:13.000000000 +0000
+++ /mnt/data/apprentice_src/GuiElementSkillTreeCanvas.cs	2026-07-14 01:23:27.318983157 +0000
@@ -119,6 +119,7 @@
 
         private bool draggingTree;
         private bool textureReady;
+        private bool disposed;
 
         private double treePanelX;
         private double treePanelY;
@@ -3470,11 +3471,10 @@
                 $"RANK {rank}/{node.MaxRank}";
 
             double rankWidth =
-                tinyFont
-                    .GetTextExtents(
-                        rankText
-                    )
-                    .Width;
+                MeasureTextWidth(
+                    tinyFont,
+                    rankText
+                );
 
             DrawText(
                 ctx,
@@ -4028,13 +4028,32 @@
             double x,
             double y)
         {
-            textUtil.DrawTextLine(
-                ctx,
-                font,
-                SafeText(text),
-                x,
-                y
-            );
+            CairoFont drawFont =
+                font.Clone();
+
+            ctx.Save();
+
+            try
+            {
+                // TextDrawUtil.DrawTextLine() does not call SetupContext().
+                // The temporary clone owns the native FontOptions created here.
+                drawFont.SetupContext(
+                    ctx
+                );
+
+                textUtil.DrawTextLine(
+                    ctx,
+                    drawFont,
+                    SafeText(text),
+                    x,
+                    y
+                );
+            }
+            finally
+            {
+                ctx.Restore();
+                drawFont.Dispose();
+            }
         }
 
         private void DrawText(
@@ -4048,7 +4067,7 @@
             double b,
             double a)
         {
-            CairoFont colored =
+            CairoFont drawFont =
                 font.Clone()
                     .WithColor(
                         new[]
@@ -4060,15 +4079,28 @@
                         }
                     );
 
-            textUtil.DrawTextLine(
-                ctx,
-                colored,
-                SafeText(text),
-                x,
-                y
-            );
+            ctx.Save();
 
-            colored.Dispose();
+            try
+            {
+                // SetupContext() must run before CairoFont.Dispose().
+                drawFont.SetupContext(
+                    ctx
+                );
+
+                textUtil.DrawTextLine(
+                    ctx,
+                    drawFont,
+                    SafeText(text),
+                    x,
+                    y
+                );
+            }
+            finally
+            {
+                ctx.Restore();
+                drawFont.Dispose();
+            }
         }
 
         private void DrawCenteredText(
@@ -4084,9 +4116,10 @@
                 );
 
             double width =
-                font.GetTextExtents(
+                MeasureTextWidth(
+                    font,
                     safe
-                ).Width;
+                );
 
             DrawText(
                 ctx,
@@ -4115,9 +4148,10 @@
                 );
 
             double width =
-                font.GetTextExtents(
+                MeasureTextWidth(
+                    font,
                     safe
-                ).Width;
+                );
 
             DrawText(
                 ctx,
@@ -4141,15 +4175,46 @@
             double y,
             double width)
         {
-            return textUtil
-                .AutobreakAndDrawMultilineTextAt(
-                    ctx,
-                    font,
-                    SafeText(text),
-                    x,
-                    y,
-                    width
-                );
+            CairoFont drawFont =
+                font.Clone();
+
+            try
+            {
+                return textUtil
+                    .AutobreakAndDrawMultilineTextAt(
+                        ctx,
+                        drawFont,
+                        SafeText(text),
+                        x,
+                        y,
+                        width
+                    );
+            }
+            finally
+            {
+                drawFont.Dispose();
+            }
+        }
+
+        private static double MeasureTextWidth(
+            CairoFont font,
+            string text)
+        {
+            CairoFont measureFont =
+                font.Clone();
+
+            try
+            {
+                return measureFont
+                    .GetTextExtents(
+                        SafeText(text)
+                    )
+                    .Width;
+            }
+            finally
+            {
+                measureFont.Dispose();
+            }
         }
 
         private static void SetColor(
@@ -4242,12 +4307,17 @@
 
         public override void Dispose()
         {
+            if (disposed)
+            {
+                return;
+            }
+
+            disposed = true;
+
+            // The five field fonts are immutable templates. All Cairo work is
+            // performed by short-lived initialized clones, so the templates do
+            // not own native FontOptions and must not be disposed.
             canvasTexture.Dispose();
-            titleFont.Dispose();
-            sectionFont.Dispose();
-            bodyFont.Dispose();
-            smallFont.Dispose();
-            tinyFont.Dispose();
 
             base.Dispose();
         }
--- /mnt/data/apprentice_original/modinfo.json	2026-07-14 00:52:13.000000000 +0000
+++ /mnt/data/apprentice_src/modinfo.json	2026-07-14 01:23:27.319363311 +0000
@@ -6,8 +6,8 @@
   "authors": [
     "Unknown"
   ],
-  "description": "Adds interactive server-authoritative skill trees using the stable autosized dialog shell and independent sizing bounds.",
-  "version": "2.1.4",
+  "description": "Adds interactive server-authoritative skill trees using the stable autosized dialog shell, safe Cairo font setup, and independent sizing bounds.",
+  "version": "2.1.5",
   "dependencies": {
     "game": "1.22.3"
   }
--- /mnt/data/apprentice_original/ApprenticeModSystem.cs	2026-07-14 00:52:13.000000000 +0000
+++ /mnt/data/apprentice_src/ApprenticeModSystem.cs	2026-07-14 01:23:27.319854988 +0000
@@ -137,7 +137,7 @@
             apiForClientLogging = api;
             api.Logger.Notification(
                 "[Apprentice] Client startup: begin — " +
-                "Interactive SkillTree Stable Shell, version 2.1.4."
+                "Interactive SkillTree Stable Shell, version 2.1.5."
             );
 
             try
